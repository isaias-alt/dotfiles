# TODO

1. ~~Al abrir nvim se repinta encima de la terminal base y las opacities se suman - molesto.~~ Resuelto: `text_background_opacity` bajado de 0.6 a 0.08 en wezterm.lua.
2. Configurar la barra lateral de archivos y las pestañas en nvim, basándose en lo que tiene folke (LazyVim).
   - Probado y revertido del todo: neo-tree.nvim, después `Snacks.picker.explorer()` (el que usa folke en `nvim/lua/plugins/snacks.lua` de folke/dot), y bufferline.nvim para las tabs. El sidebar (ambas variantes) quedó con el mismo problema de fondo: al cerrar el último archivo real, queda como única "cosa" abierta y `:q` no cierra nvim de una (necesita varios intentos) - no se pudo resolver de forma confiable. Se sacó todo - config vuelta a como estaba antes de esta tarea (solo picker de snacks: `<leader>f`/`<leader>s`/`<leader>b`, sin sidebar ni tabs de buffers).
   - ~~Antes de reintentar el sidebar: investigar si el problema del `:q` múltiple es evitable de raíz (quizás con `MiniFiles`, o revisando si hay un comando `:qa`-friendly nativo del picker) en vez de parchar con autocmds de `WinClosed`.~~ Investigado en folke/dot y craftzdog/dotfiles: ninguno de los dos usa un sidebar persistente ni tiene autocmds `WinClosed` - folke usa el mismo `Snacks.picker.explorer()` con preset `sidebar` que ya se había probado y descartado, y craftzdog usa `telescope-file-browser.nvim` como picker flotante (no split), sin quedar nunca como última "cosa" abierta. Se agregó `telescope-file-browser.nvim` en `nvim/lua/plugins/navigation.lua` (`<leader>e`), probado headless vía RPC (`nvim --server`/`--remote-send`): abrir el browser, cerrarlo con `<Esc>` y hacer `:q` sobre el archivo real cierra nvim de una, sin el problema de antes.
   - ~~Ajuste de orden en telescope-file-browser: con `grouped = true` (carpetas primero) pero el `sorting_strategy` default de telescope (`descending`), la lista se renderizaba invertida - archivos arriba, carpetas pegadas abajo contra el prompt.~~ Resuelto: agregado `sorting_strategy = 'ascending'` + `layout_config = { prompt_position = 'top' }` en `require('telescope').setup()`, para que el orden visual (arriba-abajo) coincida con el orden lógico (carpetas primero). Verificado headless via RPC leyendo el buffer `TelescopeResults`.
   - Pendiente: personalizar los atajos de teclado dentro de `telescope-file-browser.nvim` (crear/renombrar/mover/borrar archivos y carpetas) - por ahora solo tiene los defaults del plugin, sin revisar.
   - ~~Pendiente opcional (aplica igual si se retoma el sidebar): los colores se ven mejor con tokyonight que con atom-one-darker. Probar un híbrido: colores de fondo/highlight (UI) de tokyonight combinados con los colores de sintaxis de código de atom-one.~~ Hecho: colorscheme `atom-one-tokyonight` agregado a `atom-one.nvim` (fork propio) - aplica tokyonight-night completo y overlayea encima la sintaxis/código de atom-one-darker (`lua/atom-one/tokyonight_overlay.lua`). Se probó un tiempo como colorscheme activo de nvim, con `home/.config/wezterm/colors/tokyonight-night.toml` (copia del generador oficial de tokyonight.nvim) del lado de wezterm. ~~Por ahora se volvió a `atom-one-night-flat` en ambos - decisión de cuál dejar de forma definitiva sigue pendiente.~~ Decisión final: se queda `atom-one-night-flat` en nvim y wezterm.
   - ~~Probado: la variante `atom-one-night-flat` de `atom-one.nvim` (en vez de `atom-one-darker`) - confirmado en la fuente del plugin que solo cambia el fondo (`#16191d`, más oscuro) y el gris de los números de línea; los 8 colores de sintaxis son idénticos entre variantes. Sigue faltando un color scheme de wezterm dedicado a night-flat.~~ Resuelto: se agregó `home/.config/wezterm/colors/atom-one-night-flat.toml` (mismo fg/cursor/ANSI que `atom-one-darker.toml`, con `background`/`selection_bg` recalculados para el fondo `#16191d`). Nvim y wezterm quedaron alineados en esta variante.
   - ~~Nuevo error encontrado: los highlights de los diffs (neogit/diffview) no se ven bien con atom-one-darker - no hay rojo/verde visible en las líneas agregadas/eliminadas.~~ Resuelto del lado del theme (3 commits en `atom-one.nvim`: alpha de `DiffAdd`/`DiffChange`/`DiffDelete`/`DiffText` subida en las 5 variantes oficiales; para `atom-one-tokyonight` además hubo que mover el bump al hook `on_colors` de tokyonight.nvim, porque parchar los grupos después de `tokyonight.load()` no llegaba a los highlights propios de la integración de Neogit de tokyonight, ej. `NeogitDiffAddHighlight`). Igual quedó sin sentido de raíz: neogit y diffview.nvim se sacaron del todo de `git.lua` y se reemplazaron por `lazygit.nvim` (`<leader>g`), que renderiza sus propios colores de diff por fuera de los highlight groups de nvim. ~~Pendiente encontrado después: lazygit por sí solo (con o sin `lazygit.nvim`) mostraba el diff en modo unificado, sin side-by-side, y el fondo rojo/verde de las líneas no se veía (solo el texto coloreado).~~ Resuelto: `programs.lazygit` en `home.nix` configurado con `git.pagers` usando `delta --side-by-side` como pager, más `gui.sidePanelWidth = 0.2` para darle más ancho al panel de diff. Detalle completo de la investigación (varios callejones sin salida con `gui.splitDiff`, `git.diffRenderers` y `ydiff`) en `binnacle/20260906-lazygit-side-by-side-diff.md`.
3. ~~Buscar una página para aprender vim.~~ Resuelto: [OpenVim](https://openvim.com/) - tutorial interactivo gratis y open source (MIT), corre en el navegador sin cuenta, más un [sandbox](https://openvim.com/sandbox.html) con ayuda contextual de comandos.
4. ~~Hacer que los colores de la cmd line (prompt) se vean mejor.~~ Resuelto: colores de `programs.starship` en `home.nix` recoloreados con la paleta de `atom-one-night-flat` (directory azul `#4aa5f0`, git_branch magenta `#c162de`, git_status naranja `#d18f52`, character verde `#8cc265`/rojo `#e05561`, cmd_duration naranja `#d18f52`), consistente con nvim/wezterm/herdr.
5. ~~Las respuestas de Claude Code se ven más opacas/apagadas que los mensajes ya-enviados del usuario - solo en WezTerm, no en Warp/Terminal.app.~~ Resuelto tras una investigación larga con muchos callejones sin salida: `window_background_opacity`, `text_background_opacity`, 3 colorschemes distintos (atom-one/tokyonight/rose-pine-moon), actualizar WezTerm a un build nightly (el fix real de ese build era sobre síntesis de peso de fuente variable, no aplicaba a esta fuente estática), cambiar de fuente a JetBrainsMono, `font_rules`/`freetype_load_target`/`freetype_render_target`, comparar contra Ghostty (mismo resultado - descartó el renderer), investigar el `[theme]` de herdr (solo afecta su propio chrome, no el contenido de los panes), y `front_end = "OpenGL"` - ninguno cambió nada. La causa real: el cuerpo de mi respuesta se manda **sin código de color** (foreground implícito/default), confirmado leyendo los bytes ANSI reales de la sesión con `herdr pane read --format ansi` (la única forma de ver el contenido real - `wezterm cli get-text` siempre leía el frame externo de herdr, nunca el pane interno). Ese foreground implícito toma el valor `foreground` del colorscheme activo de WezTerm (`#abb2bf`, bastante apagado), mientras que el mensaje ya-enviado del usuario usa un color explícito y más brillante. Fix: `foreground` subido a `#e6e6e6` en `home/.config/wezterm/colors/atom-one-night-flat.toml`. Efecto secundario encontrado y arreglado en el camino: el `nvim_set_hl(0, group, { bg = 'none' })` usado para hacer transparente el fondo de nvim (tarea 1) reemplazaba la definición entera del highlight en vez de mergear, borrando el `fg` propio de `LineNr` y dejando los números de línea en blanco - corregido leyendo el highlight existente con `nvim_get_hl` antes de reescribirlo. De paso, `userMessageBackground` (Claude Code) se fijó en `#16191d` (mismo fondo de la terminal) vía `home/.claude/themes/atom-one-night-flat.json`, y `text_background_opacity` quedó sin setear (default `1.0`) para que las tabs de herdr se vean con más contraste - el problema de opacities-que-se-suman en nvim que ese valor bajo evitaba ya no aplica, porque nvim dejó de pintar fondo propio.